import Evaluation from '../models/Evaluation';
import User from '../models/User';

class PromoterAvarage {
  async index(req, res) {
    return res.json();
  }
}

export default new PromoterAvarage();
